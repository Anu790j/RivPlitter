// script.js - No animations
document.addEventListener('DOMContentLoaded', () => {
    // No JS animations or smooth scrolling logic required.
    // Native browser behavior will handle anchor links (jumping instantly).
});
